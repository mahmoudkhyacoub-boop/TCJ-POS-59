# تقرير التدقيق المحاسبي والتقني الشامل — V206

## النتيجة التنفيذية

تمت مراجعة النسخة الحالية بعد تطبيق رسوم الفيزا على خروج الحوالة، اعتماد خط Cocon® Next Arabic Bold، وتحويل ألوان النصوص الحمراء إلى الأبيض. شمل الفحص مسارات العمليات، القيود، التعديل والحذف وإعادة الترحيل، التقارير، السيولة، المخزون، الصلاحيات، هوية المحل، الموارد، وسلامة قاعدة البيانات.

نتيجة الاختبارات المتاحة: **9 اختبارات ناجحة من أصل 9 دون فشل**. كما نجح فحص الصياغة وAST وفحص المسارات الأساسية.

## نتائج الفحص

| المجال | النتيجة | التفسير |
|---|---|---|
| المبيعات | ناجح | مسار `checkout` وترحيل الإيراد وتكلفة المخزون موجودان |
| المشتريات | ناجح | مسار الحفظ ومصدر التمويل وترحيل المخزون موجود |
| الصيانة | ناجح | الإيراد وتكلفة قطع الصيانة ومسار السجل مستقلان |
| دخول الحوالة | ناجح | العمولة والحسابات ومسار إعادة الترحيل موجودة |
| دفع الفواتير | ناجح | مسار العملية ومعادلة الفيزا موجودان |
| خروج الحوالة بالفيزا | ناجح | أصبحت المعادلة القيمة + 0.9% + العمولة، مع توحيد الإنشاء والتعديل وإعادة الترحيل |
| خروج الحوالة عبر CLIQ | ناجح ضمن فحص النطاق | لم تُطبق رسوم 0.9% على المسار غير الفيزا |
| النقد | ناجح ضمن فحص النطاق | بقي المسار النقدي على سلوكه السابق |
| القيود | ناجح | توجد دوال ترحيل وإبطال وعكس وإعادة بناء للقيود |
| المرتجعات والتالف | ناجح ساكنياً ووظيفياً ضمن الاختبارات المتاحة | يوجد مسار `inventory_adjustments` وربط بالعملية الأصلية |
| التقارير والأرباح | ناجح | توجد التقارير والفلترة ومسارات السيولة والمطابقة |
| العملاء والذمم | ناجح | يوجد حفظ بالمعرف الداخلي وتحديث الهاتف والبيانات المرتبطة |
| الصلاحيات | ناجح | يوجد تحميل وحفظ للصلاحيات الفردية وإدارة المستخدمين |
| الخط | ناجح | `APP_FONT_FAMILY` يعتمد Cocon® Next Arabic والملف موجود |
| ألوان النصوص | ناجح | لم تبقَ استخدامات النص الأحمر المباشرة؛ ألوان الأزرار والخلفيات والرسومات محفوظة |

## التحقق المحاسبي

تم التحقق من وجود صيغة رسوم الفيزا في دالة الإجمالي، ومسار إضافة الحوالة، ومسار إعادة الترحيل، ومسار تعديل السجل. المثال الحسابي المستخدم هو: قيمة 100 + رسوم 0.90 + عمولة 2 = إجمالي 102.90 دينار. كما تم التحقق من أن مسارات النقد وCLIQ لا تدخل ضمن شرط رسوم الفيزا.

## التحقق التقني

نجح تحليل AST، ونجح `py_compile`، وتم التحقق من وجود 20 دالة تشغيلية أساسية تشمل المصادقة والمبيعات والمشتريات والحوالات والمصروفات والمخزون والتقارير والعملاء والإعدادات والصلاحيات. كما نجح اختبار واجهة الدخول المستعادة، ونجحت اختبارات الموارد والرسومات والصياغة العربية ومسارات سجل الصيانة.

## قاعدة البيانات

تم فتح قاعدة البيانات الحالية والنسخ الاحتياطية بوضع القراءة فقط. جميع القواعد المفحوصة تحتوي على 27 جدولاً، ونتيجة `PRAGMA integrity_check` هي `ok`، ونتيجة `PRAGMA foreign_key_check` هي صفر مخالفات. لكن قواعد البيانات المتاحة في مساحة العمل لا تحتوي على حركات تاريخية في جداول المبيعات والمشتريات والصيانة والحوالات والقيود والمصروفات والمخزون؛ ولذلك يثبت هذا الفحص السلامة البنيوية فقط، ولا يثبت تطابق الأرصدة التاريخية للمحل.

## حدود التدقيق

لم يتم تشغيل EXE على Windows فعلياً داخل جهاز المحل، بل تم اختبار التطبيق في بيئة Linux رسومية افتراضية، مع فحص ملفات البناء والموارد. كما أن التدقيق الرقمي التاريخي يتطلب نسخة قاعدة بيانات المحل الفعلية التي تحتوي على الحركات. لذلك لا يجوز اعتبار أرصدة التقارير التاريخية مؤكدة قبل فحص تلك النسخة قراءةً فقط.

## الخلاصة

حسب الكود والاختبارات وقواعد البيانات المتاحة، النتيجة **ناجحة دون فشل مثبت**. تم التحقق من تعديل فيزا خروج الحوالة، خط Cocon® Next Arabic Bold، تحويل النصوص الحمراء إلى الأبيض، وبقاء المسارات الأخرى ضمن نطاقها. الملاحظة الوحيدة المتبقية هي ضرورة اختبار EXE على Windows وفحص قاعدة بيانات المحل الحقيقية للتأكد من الأرقام التاريخية الفعلية.
