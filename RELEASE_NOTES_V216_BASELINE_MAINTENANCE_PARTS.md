# V216 — مرجع أساسي جديد وتكلفة قطع الصيانة

## زر تثبيت مرجع أساسي جديد

تمت إضافة زر في الجهة اليسرى من صف الإجراءات داخل شاشة تثبيت ومقارنة الوضع المالي باسم **تثبيت مرجع أساسي جديد**. يفتح الزر حقول المرجع الحالي للتعديل، ثم يحفظ صفاً جديداً من النوع baseline بدلاً من حذف أو استبدال المرجع القديم.

بعد الحفظ، يستخدم النظام أحدث مرجع أساسي كنقطة بداية، وتُحتسب حركة القيود بعد لحظة حفظ المرجع فقط. السجلات المحاسبية والحركات السابقة لا يتم تعديلها أو حذفها.

## تكلفة قطع الصيانة

تمت إضافة حقل `maintenance_parts_cost` إلى سجل اللقطات بقيمة افتراضية صفرية للبيانات القديمة. تُحسب قيمة قطع الصيانة الحالية من:

`SUM(cost_price × stock)` من جدول `maintenance_parts`.

يظهر الحقل في المرجع الأساسي واللقطة الحالية وجدول المقارنة ويدخل ضمن إجمالي الموجودات. إضافة العمود غير تدميرية وتحافظ على كل الأعمدة والسجلات السابقة.

## التحقق

نجح فحص الصياغة، اختبار V216 الثابت، اختبار V216 الديناميكي المعزول، فحص القيود المحاسبية السابق، وفحص V215. تم التحقق من سلامة إدخالات baseline/current ومن عدم وجود كتابة على جداول العمليات أثناء الاختبار.
