# Trend Center Jordan — V203 Login Black Screen Fix

## السبب الجذري

كانت صورة الخلفية تُفك بنجاح داخل الذاكرة، لكن عرضها كان يتم عبر `CTkLabel` مع كائن `ImageTk.PhotoImage`. في بعض بيئات Windows وPyInstaller لا يعرض هذا الاقتران الصورة بشكل موثوق، فتظهر طبقة سوداء بينما تبقى حقول الدخول ظاهرة.

## الإصلاح

تم استبدال عنصر الخلفية بعنصر `tk.Label` القياسي المخصص لعرض `PhotoImage`. بقيت الصورة المرجعية مضمنة داخل `main.py` بصيغة Base64، وبقيت الخلفية وطبقة الحقول داخل إطار 16:9 واحد. كما تم إبقاء fallback مرئي في حال تعذر فك الصورة بدلاً من عرض شاشة سوداء صامتة.

## نطاق التعديل

تم تعديل مسار عرض واجهة الدخول فقط. لم تتغير دالة المصادقة أو المستخدمون أو الصلاحيات أو العمليات المحاسبية أو بقية الواجهات.

## التحقق

```text
Embedded WEBP payload: PASS — 2048x1152
Native Tk background widget: PASS
16:9 artwork/form shared frame: PASS
Python syntax: PASS
Regression tests: 7 PASS / 0 FAIL
```

يجب بناء EXE جديد من هذه الحزمة وتشغيله بدلاً من النسخ القديمة.
